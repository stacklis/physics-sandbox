{
  "name": "physics-sandbox",
  "version": "1.0.2",
  "description": "Physics Sandbox — learn by playing",
  "scripts": {
    "dev": "npx serve -l tcp://0.0.0.0:8081",
    "start": "npx serve -l tcp://0.0.0.0:8081",
    "test": "node --test tests/**/*.test.mjs",
    "generate-icons": "python scripts/generate_icons.py"
  },
  "keywords": [
    "physics",
    "sandbox",
    "simulation"
  ],
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0"
  }
}
